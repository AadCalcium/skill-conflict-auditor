interface:
  display_name: "Skill Conflict Auditor"
  short_description: "Find agent skill conflicts and review patches"
  brand_color: "#2563EB"
  default_prompt: "Use $skill-conflict-auditor to audit my Claude Code, Codex, OpenClaw, or other agent skills and generate reports plus a suggested patch without applying changes."
policy:
  allow_implicit_invocation: true
